User: <%= current_user.login %><br>
<%= link_to_remote 'Logout', :url => logout_path %>

<%= javascript_tag "$('account_box_label').update('Welcome, #{current_user.login}');boxRefresh('payment_box');"  %>
